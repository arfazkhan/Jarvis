groq
openai
pydantic
python-dotenv
requests

# Phase 1: Cognitive Layer (Memory + Prediction)
faiss-cpu                    # Vector similarity search
sentence-transformers        # Text embeddings
chromadb                     # Alternative vector store
numpy                        # Numerical operations
scikit-learn                 # ML utilities

# Phase 2: Natural Interaction (Conversation + Voice)
openai-whisper              # Speech-to-text (local)
pyttsx3                     # Text-to-speech (local fallback)
textblob                    # Sentiment analysis
nltk                        # NLP utilities
SpeechRecognition           # Voice recognition abstraction
RealtimeTTS                 # Streaming TTS (Coqui, Kokoro, Piper)
emoji                       # Emoji handling for TTS text sanitization
sounddevice                 # Audio I/O for CosyVoice

# CosyVoice TTS (optional - high-quality, low-latency)
# Requires separate installation:
#   git clone https://github.com/FunAudioLLM/CosyVoice.git
#   cd CosyVoice && pip install -e .
# Then set TTS_ENGINE=cosyvoice in .env

# Phase 3: Autonomous Planning (Multi-step Execution)
networkx                    # Graph operations for dependency management
graphviz                    # Plan visualization
# pydantic is already listed as a core dependency, but also relevant here for data validation

# Phase 4: Sensor Fusion (Perception)
scipy                       # Scientific computing
filterpy                    # Bayesian filters (Kalman, particle filters)
pandas                      # Data manipulation for sensors

# Phase 5: Personality (Emotional AI)
transformers                # Advanced sentiment models

# Qatar Calendar Support
hijri-converter             # Accurate Islamic calendar for Ramadan detection

# Phase 6: Missions (Long-running Autonomy)
apscheduler                 # Mission scheduling
python-crontab              # Cron-style scheduling
croniter==2.0.1             # Existing croniter dependency

# BMS Protocol Adapters
pymodbus>=3.5.0              # Modbus TCP/RTU async client
aiohttp>=3.9.0               # Async HTTP (CAFM: Maximo, Planon, REST)

# Phase 7: Web UI (Reasoning Console)
flask                       # Web framework
flask-cors                  # CORS support
flask-socketio              # Real-time updates
plotly                      # Interactive visualizations
dash                        # Dashboard framework

# Testing & Quality
pytest==7.4.3               # Testing framework
pytest-cov==4.1.0           # Coverage reporting
pytest-asyncio==0.21.1      # Async testing
black                       # Code formatting
flake8                      # Linting
mypy                        # Type checking
pytest-xdist==3.5.0
pytest-timeout==2.2.0
pytest-mock==3.12.0
requests-mock==1.11.0
faker==20.1.0
croniter==2.0.1
python-jose[cryptography]>=3.3.0
bacpypes3>=0.0.85
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
boto3>=1.34.0
