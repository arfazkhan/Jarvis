# ArvisX pilot — base API image deps (the deterministic floor + REST + MQTT).
# The LLM layer's provider SDKs live in requirements-llm.txt; the vendored
# commercial packages (agent_home/agent_unified) are copied in by the Dockerfile.
fastapi>=0.110
uvicorn[standard]>=0.27
paho-mqtt>=2.0
python-dotenv>=1.0
